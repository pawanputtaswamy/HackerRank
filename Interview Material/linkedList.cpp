using namespace std;

#include<iostream>
#include "LinkedListOperation.h"

/*
int main()
{
	linkedList* li = new linkedList();

	int arr[] = { 30, 20, 10 };
	int size = sizeof(arr) / sizeof(arr[0]);

	for (int i = 0;  i < size; i++)
	{
		li->insertAtFront(arr[i]);
	}
	
	//li->printList();
	cout << endl << "After Inserting 40:" <<endl;
	li->insertAtEnd(40);
	li->printList();
	li->reverseLinkedList();
	cout << endl << "After Reversing:" << endl;
	li->printList();

	return 0;
	}*/